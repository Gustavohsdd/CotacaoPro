rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // mais seguro para dev: exige usuário autenticado (pode ser anônimo)
      allow read, write: if request.auth != null;
    }
  }
}
